# Dhad — Project Handoff Document (v4)

**As of:** End of v1.2.0 documentation session  
**Owner:** Chokri Abouzid  
**Repository:** https://github.com/chokriabouzid-star/dhad  
**Crate:** https://crates.io/crates/dhad  
**Latest published version:** `v1.1.2`  
**Active development version:** `v1.2.0` (pre-release)

---

## 1. Project summary

Dhad is a deterministic Arabic text canonicalization library in Rust.

It converts Arabic Unicode input into a canonical `AtomStream` of fixed
8-byte atoms, then derives two domain-separated SHA-256 hashes:

- **CoreHash** — orthographic identity (base + marks + flags)
- **PhoneticHash** — prosodic identity (CoreHash + prosody)

Two modes:

- **Mode A** — UTF-8 Arabic text input
- **Mode B** — tagged binary frame (for pre-annotated MADD-style content)

Design axioms: A1, A2, A4, A5, A7, A8.

---

## 2. Released versions

| Version | Status | Notes |
|---------|--------|-------|
| 1.0.0 | published | legacy |
| 1.0.1 | published | legacy |
| 1.1.0 | published | hardening + honesty docs |
| 1.1.1 | published | packaging fix |
| 1.1.2 | published | test-contract reinforcement |
| 1.2.0 | in progress | conformance corpus + Python verifier |

---

## 3. Repository invariants

- `cargo fmt --check` clean
- `cargo clippy --all-targets` clean
- `cargo test` — 284 tests, all green
- `cargo run --example export_vectors` — 185 vectors generated into `../dhad-conformance-suite/vectors/`
- `python3 ../dhad-conformance-suite/tools/verify_vectors.py` — ALL VECTOR FILES VERIFIED
- `python3 ../dhad-conformance-suite/python_ref/verify_tagged_ref.py` — 30/30
- `python3 ../dhad-conformance-suite/python_ref/verify_golden_ref.py` — 155/155
- No `unsafe`
- Stable Rust; nightly only for `cargo-fuzz`

---

## 4. What is completed

### 4.1 v1.1.x (closed)

- Bug fixes (duplicate prosody, checked arithmetic, MAX_INPUT_BYTES)
- NFC policy documented and enforced in `suite6_nfc_rejection.rs`
- `CONFORMANCE.md` tracked in git and shipped in crate
- `suite5_coverage.rs` rewritten as behavioral assertions
- Mode B structural error contract pinned

### 4.2 v1.2.0 conformance infrastructure (in progress on `feature/v1.2-python-ref`)

#### Step 1–2: Schema design ✅
- 7 design questions answered
- canonical schema published in `dhad-conformance-suite/schema/vector-schema-1.0.md`

#### Step 3: Manifests ✅
- `tests/cases/golden_cases.rs` — 116 Mode A success vectors
- `tests/cases/tagged_cases.rs` — 30 Mode B vectors
- `tests/cases/adversarial_cases.rs` — 39 Mode A vectors
- `tests/cases/mod.rs`

#### Step 3B: Exporter ✅
- `examples/export_vectors.rs`
- Generates `../dhad-conformance-suite/vectors/{golden,adversarial,tagged}.json`
- Validates all vectors against manifests during export
- Schema v1.0 with `hex` and `repeat_byte` input encodings

#### Step 4A: Python verifier ✅
- `tools/verify_vectors.py`
- Independent CoreHash/PhoneticHash recomputation from `stream_hex`
- Schema structure validation
- Anchor verification (4 mandatory anchors)
- Error object field validation per `ErrorKind`
- **First cross-language hash verification in the project**

#### Step 4B: Python reference implementation ✅
- Implemented in `dhad-conformance-suite/python_ref/`
- Mode B: 30/30 exact parity
- Mode A: 155/155 exact parity
- Full corpus parity: **185/185**
- Python 3.10+ stdlib only, no Rust dependency

#### Documentation ✅
- Schema published in `dhad-conformance-suite/schema/vector-schema-1.0.md`
- CONFORMANCE.md, README.md, HANDOFF.md updated for release

---

## 5. Branch structure
main
└── feature/v1.2-exporter (merged to main, closed)
└── feature/v1.2-python-ref (active)
└── docs/v1.2-schema-and-conformance (active — this session)

text


After documentation is merged, the next branch will be:
feature/v1.2-python-ref → add Python reference implementation

text


---

## 6. Conformance vector corpus

| File | Mode | ok | err | Total |
|------|------|----|-----|-------|
| `golden.json` | A | 116 | 0 | 116 |
| `adversarial.json` | A | 3 | 36 | 39 |
| `tagged.json` | B | 9 | 21 | 30 |
| **Total** | | **128** | **57** | **185** |

Generated by: `cargo run --example export_vectors`  
Verified by: `python3 tools/verify_vectors.py`  
Schema: `dhad-conformance-suite/schema/vector-schema-1.0.md`

---

## 7. What is NOT yet done

### v1.2.0 remaining
- Release `v1.2.0` on crates.io
- Tag the release in both repositories

### v1.3.0 — not started
- `process_mode_a_with_profile(input, profile)`
- `enum Profile { Strict, QuranicRelaxed }`
- Re-introduce `quran` keyword

### v2.0.0 — not started
- `ErrorKind::MalformedFrame { byte_offset, reason }`
- `#[non_exhaustive]` on `ErrorKind`
- DhadIR (word-level IR)
- Wethaq (signing/verification layer)

---

## 8. Open issues

1. Decomposed U+0653/U+0654/U+0655 — documented as known limitation
2. Dedicated `MalformedFrame` error — planned v2.0
3. Second non-Python implementation
4. Future corpus expansion beyond the current 185 vectors

---

## 9. Known limitations (unchanged from v1.1.2)

- NFC-precomposed Arabic required
- U+0653, U+0654, U+0655 → `UnmappedCodepoint`
- U+06D6–U+06ED → out of scope for strict Mode A
- Mode B errors reported as `MalformedUtf8` in v1.x
- Some pipeline stages are identity stages in v1.x

---

## 10. Engineering principles

- One branch per intent
- Failing test first, then fix
- `--no-ff` merges
- `cargo publish --dry-run` before publish
- Documentation lies are bugs
- Spec, tests, and code must agree

---

## 11. How to resume

Open a new chat and paste:

> I am continuing work on **Dhad** (https://crates.io/crates/dhad).
> Repository: https://github.com/chokriabouzid-star/dhad
> Conformance suite: https://github.com/chokriabouzid-star/dhad-conformance-suite
> Python reference implementation now matches **185/185** published vectors.

Then paste this document.

---

## 12. Anchor constants (sacred — never change)

| Anchor | Stream | CoreHash | PhoneticHash |
|--------|--------|----------|--------------|
| ANCHOR-001 | `""` | `8dd837c6...` | `c5f62e92...` |
| ANCHOR-002 | `0100000000000000` | verified | verified |
| ANCHOR-003 | `0200000000000000` | verified | verified |
| ANCHOR-004 | `0200010000000000` | verified | verified |

Full hash values in `tools/anchor_verify.py` and `tools/verify_vectors.py`.
