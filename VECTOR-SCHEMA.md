# Dhad Vector Schema

This repository no longer carries the canonical conformance schema text.

The authoritative published schema for Dhad conformance vectors lives in:

- https://github.com/chokriabouzid-star/dhad-conformance-suite
- `schema/vector-schema-1.0.md`

Local generation/export workflow in this repository writes vector artifacts to
the sibling checkout:

- `../dhad-conformance-suite/vectors/`

See also:

- `examples/export_vectors.rs`
- `CONFORMANCE.md`
- `HANDOFF.md`
